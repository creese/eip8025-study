Patch `notes/refs.md` per audit. Metadata only; no content summaries. Do not commit — I will review `git diff` and commit manually.

1. `gh pr view 11604 --repo ethereum/EIPs --json baseRefOid,isDraft` — record base SHA and isDraft in the EIP PR block. Then in the EIPs clone: `git merge-base <baseRefOid> d5653bc4d9b86997e069567dcd1eb8766b0c8a55`; record as the EIP PR merge-base.
2. In the Lighthouse clone: `git merge-base dfb259171a65cacd6db57b8874af8f543cabcb7a 0dd6c3b8cf3b1eece82a0a7ee87282a222d93bf5` — record in the LH block as "merge-base vs PR base branch". Add to the discrepancy block: "PR head appears to carry upstream commits past the stated base, so the Phase 2a diff base must be chosen and logged carefully. Candidate baselines recorded for that decision: the merge-base vs PR base branch, and the merge-base vs sigp/lighthouse@unstable (`494b00a3...`). Decision deferred to the Phase 2a decision log."
3. `gh pr view 39 --repo eth-act/lighthouse --json body` — scan the body only for consensus-specs references (branch/commit/PR links). Record one line: found <ref> / none found. Add a matching one-line record for feature-flag names already observed. Do not summarize any other body content.
4. `git ls-remote https://github.com/ethereum/consensus-specs refs/tags/v1.7.0-alpha.8 refs/tags/v1.7.0-alpha.8^{}` — record repo URL, tag name, and dereferenced commit SHA.
5. Rewrite the consensus-specs conclusion: the v1.7.0-alpha.8 pin is inherited, unchanged by PR #39, and no EIP-8025-specific spec ref was found → mark **PROVISIONAL-SPEC-BASELINE** (provisional: yes). Keep the ef-tests pin as supporting metadata.
6. Append to `notes/open-questions.md`: `Q1 | 2026-07-12 | Phase 0 | What consensus-specs ref (branch/commit) does eth-act/lighthouse PR #39 target for EIP-8025 content? No EIP-8025-specific spec reference found in PR body, doc-comments, or vendored paths; only an inherited general ef-tests pin (v1.7.0-alpha.8). Needs author/maintainer confirmation. | open`
7. Stop after patching. List the files you changed; do not commit.
