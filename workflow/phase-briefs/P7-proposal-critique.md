# Phase 7 design brief — proposal critique

**Status:** Authoritative drafting source until
`phases/P7-proposal-critique.md` exists as an executable phase
specification under the rules in `CLAUDE.md`.

## Retained v3.2 drafting source
**P7 — Draft critique (Chat):**
> Here's `notes/draft/proposal.md` section `<X>` plus the matrices. Do not rewrite. List every claim not traceable to a matrix row, a `[raw:<path>]` pointer, or an existing `[OPEN-Qn]` tag. After I explicitly request prose tightening, you may edit the file.

## Retained v3.2 completion goal
`git diff` review of every model edit shows zero new/strengthened claims; final draft has zero claims lacking a row ID, raw pointer, or an existing `[OPEN-Qn]` tag
